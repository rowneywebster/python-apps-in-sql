[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
requests = "==2.31.0"

[dev-packages]
pytest = "*"

[requires]
python_version = "3.8"

[scripts]
start = "python main.py"
test = "pytest"
